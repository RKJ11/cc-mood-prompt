# Shell scripts must use LF line endings on every OS, otherwise bash fails
# with "$'\r': command not found". This overrides any local core.autocrlf.
*.sh text eol=lf

# Signal definitions and JSON are read by the hooks; keep them LF too.
*.json text eol=lf
signals/*.md text eol=lf
